import { SatinizeHtmlPipe } from './satinize-html.pipe';

describe('SatinizeHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new SatinizeHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
